# End-To-End-ETL-Star_Schema
This GitHub repo hosts an end-to-end T-SQL pipeline using the **Medallion Architecture**. It automates ingesting messy CRM and ERP CSV data into **Bronze** staging, cleanses and standardizes it in **Silver**, and loads it into an optimized **Gold Star Schema** for enterprise BI reporting.
